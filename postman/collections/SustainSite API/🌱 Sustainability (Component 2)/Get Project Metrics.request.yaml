$kind: http-request
description: "Access: Project members only."
url: "{{baseUrl}}/api/sustainability/projects/{{projectId}}/metrics"
method: GET
headers:
  Authorization: Bearer {{token}}
order: 6000
