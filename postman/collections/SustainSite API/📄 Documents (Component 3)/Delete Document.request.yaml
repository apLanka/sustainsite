$kind: http-request
description: |-
  Access: Document owner (uploadedBy) or ADMIN.

  Also deletes the file from Cloudinary.
url: "{{baseUrl}}/api/documents/{{documentId}}"
method: DELETE
headers:
  Authorization: Bearer {{token}}
order: 5000
